<UserControl xmlns:ProberSimulator="clr-namespace:ProberSimulator;assembly=ProberSimulator"  x:Class="Viewer3DModel.Viewer3DModelV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Viewer3DModel"
             xmlns:ps="clr-namespace:ProberSimulator;assembly=ProberSimulator"
             mc:Ignorable="d" d:DesignWidth="450" Height="450">
    <Grid>

            <!--<ContentControl DataContext="{Binding}" Content="{Binding Stage3DModel}" Margin="2" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>-->
        <ps:OPUS5_3D HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                CamPosition="{Binding CamPosition}"
                CamLookDirection="{Binding CamLookDirection}"
                CamUpDirection="{Binding CamUpDirection}"
                />

        
        
    </Grid>
</UserControl>
