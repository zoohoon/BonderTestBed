<UserControl x:Class="ProberViewModel.View.ResultMap.ResultMapAnalyzeViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProberViewModel.View.ResultMap"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <TabControl Background="Gray" x:Name="ResultMapTabControl" SelectedIndex="{Binding ResultMapTabSelectedIndex}">

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding SelectedItemChangedCommand}"/>
                </i:EventTrigger>

            </i:Interaction.Triggers>
            
            <TabItem Header="STIF">
                <ContentControl DataContext="{Binding ''}" Content="{Binding InnerScreenView}"/>
            </TabItem>

            <TabItem Header="E142">
                <ContentControl DataContext="{Binding ''}" Content="{Binding InnerScreenView}"/>
            </TabItem>
        </TabControl>

    </Grid>
</UserControl>
