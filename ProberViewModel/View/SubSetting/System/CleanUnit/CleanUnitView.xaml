<UserControl x:Class="UcCleanUnitView.CleanUnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:param="clr-namespace:RecipeEditorControl.RecipeEditorParamEdit;assembly=RecipeEditorControl"
             xmlns:cui="clr-namespace:CUI;assembly=CUI"
             xmlns:local="clr-namespace:UcCleanUnitView"
             mc:Ignorable="d" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             d:DesignHeight="1500" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SettingControlStyle;component/SettingViewStyleDictionaly.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>

        <Label  Content="Devices > Clean Unit "
                Foreground="White"
                FontWeight="Bold"
                FontSize="32"      
                Grid.Column="0" Grid.Row="0"                
                />
        <Separator Margin="4" Background="White" Grid.ColumnSpan="2" 
                   VerticalAlignment="Bottom"/>

        <param:UcRecipeEditorParamEdit Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="3" DataContext="{Binding RecipeEditorParamEdit}"/>
        
        <Grid Grid.Column="0" Grid.Row="1" VerticalAlignment="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid  HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="0">
                <cui:Button Margin="32 16 0 0" GUID="8d2cd7a9-caf5-4bb0-89f5-3b6642d26fb6"
                                Content="Clean Sheet Setup" FontSize="18" Foreground="White" Height="42" Width="200"
                                Command="{Binding CleanSheetSetupCommand}" 
                                CommandParameter="{Binding RelativeSource={RelativeSource Self}}"
                                Style="{DynamicResource NormalTypeCUIButtonStyle}"
                                >
                    <cui:Button.AvoidLockHashCodes>
                        <sys:Int32>31285075</sys:Int32>
                    </cui:Button.AvoidLockHashCodes>
                </cui:Button>
            </Grid>
            <!--<Grid HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1">
                <cui:Button Margin="32 16 0 0" GUID="26168D6E-408A-8D81-28BF-4A77F4A3599E"
                                Content="Touch Sensor Setup" FontSize="18" Foreground="White" Height="42" Width="200"
                                Command="{Binding CleanSheetSetupCommand}" 
                                CommandParameter="{Binding RelativeSource={RelativeSource Self}}"
                                Style="{DynamicResource NormalTypeCUIButtonStyle}"/>
            </Grid>-->
        </Grid>
    </Grid>
</UserControl>
