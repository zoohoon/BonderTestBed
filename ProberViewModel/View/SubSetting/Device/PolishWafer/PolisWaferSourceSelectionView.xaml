<UserControl x:Class="PolishWaferSourceSettingViewProject.UC.PolisWaferSourceSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PolishWaferSourceSettingViewProject.UC"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <System:Double x:Key="DialogTitleFontSize">26</System:Double>
            <System:Double x:Key="DialogMessageFontSize">15</System:Double>
            <Style x:Key="TitleLabel" TargetType="Label">
                <Setter Property="Margin" Value="4 8"/>
                <Setter Property="Background" Value="DodgerBlue"/>
                <Setter Property="FontSize" Value="16" />
                <Setter Property="Foreground" Value="#FFE8E8E8"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>
            <Style x:Key="DefaultButton" TargetType="Button">
                <Setter Property="Margin" Value="4 20 4 4"/>
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontWeight" Value="Bold"/>
            </Style>
            <Style x:Key="WaferButton" TargetType="{x:Type ToggleButton}">
                <Setter Property="Margin" Value="8 4"/>
                <Setter Property="FontSize" Value="16" />
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Border VerticalAlignment="Center" x:Name="border"
                                        Padding="8 8" CornerRadius="4" Background="#C9C9C9" 
                                        BorderBrush="#FF000000" BorderThickness="1,1,1,1" SnapsToDevicePixels="True">

                                <ContentPresenter x:Name="contentPresenter" HorizontalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="true">
                                    <Setter Property="Background" TargetName="border" Value="#ACFF12"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <local:CountNumConverter x:Key="CountNumConverter"/>
            <local:MultiplyFormulaStringConverter x:Key="MultiplyFormulaStringConv"/>
            
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Casette slot" Style="{StaticResource TitleLabel}"/>

            <Grid Grid.Row="1">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition />
                </Grid.RowDefinitions>

                <ItemsControl Grid.Row="0" ItemsSource="{Binding SelectedCassetteInfo.CassetteSlotBtn}" 
                              AlternationCount="{Binding SelectedCassetteInfo.CassetteSlotBtn.Count}"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled" Height="620">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Vertical" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                       HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Width="Auto"
                                          Margin="14 8"
                                          Content="{Binding (ItemsControl.AlternationIndex),
                                                RelativeSource={RelativeSource Mode=TemplatedParent},
                                                Converter={StaticResource CountNumConverter}}"
                                          Style="{StaticResource WaferButton}" 
                                          IsChecked="{Binding isChecked}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{Binding Command}"
                                                   CommandParameter="{Binding CommandParameter}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">

                    <Button Width="60" Height="45">
                        <Button.Content>
                            <Viewbox Grid.Row="0" >
                                <Canvas Width="24" Height="24">
                                    <Path Data="M1,21H23L12,2" Fill="Black">
                                        <Path.LayoutTransform>
                                            <RotateTransform Angle="270"></RotateTransform>
                                        </Path.LayoutTransform>
                                    </Path>
                                </Canvas>
                            </Viewbox>
                        </Button.Content>
                    </Button>

                    <Label Foreground="White">
                        <Label.Content>
                            <TextBlock>
                                <TextBlock.Text>
                                    <!--<MultiBinding StringFormat="{}{0} / {1}">
                                        <Binding Path="SelectCassetteNumber" />
                                        <Binding Path="CassetteCount" />
                                    </MultiBinding>-->

                                    <MultiBinding Converter="{StaticResource MultiplyFormulaStringConv}">
                                        <Binding Path="SelectCassetteNumber" />
                                        <Binding Path="CassetteCount" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Label.Content>
                    </Label>

                    <Button Width="60" Height="45">
                        <Button.Content>
                            <Viewbox Grid.Row="0" >
                                <Canvas Width="24" Height="24">
                                    <Path Data="M1,21H23L12,2" Fill="Black">
                                        <Path.LayoutTransform>
                                            <RotateTransform Angle="90"></RotateTransform>
                                        </Path.LayoutTransform>
                                    </Path>
                                </Canvas>
                            </Viewbox>
                        </Button.Content>
                    </Button>

                </StackPanel>
            </Grid>

        </Grid>

        <Grid Grid.Column="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Fixed tray" Style="{StaticResource TitleLabel}"/>
            <Grid Grid.Row="1">

                <ItemsControl Grid.Column="1" ItemsSource="{Binding FixedTrayInfos}" AlternationCount="{Binding FixedTrayInfos.Count}"
                ScrollViewer.VerticalScrollBarVisibility="Disabled" >
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Vertical" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                       HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Width="Auto"
                                          Margin="8"
                                          Content="{Binding (ItemsControl.AlternationIndex),
                                                RelativeSource={RelativeSource Mode=TemplatedParent},
                                                Converter={StaticResource CountNumConverter}}"
                                          Style="{StaticResource WaferButton}" 
                                          IsChecked="{Binding isChecked}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{Binding Command}"
                                                   CommandParameter="{Binding CommandParameter}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

        </Grid>

        <Grid Grid.Column="2">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="Inspection tray" Style="{StaticResource TitleLabel}"/>
            <Grid Grid.Row="1">
                <ItemsControl Grid.Column="1" ItemsSource="{Binding InspectionTrayInfos}" AlternationCount="{Binding InspectionTrayInfos.Count}"
                ScrollViewer.VerticalScrollBarVisibility="Disabled" >
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Vertical" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                       HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Width="Auto"
                                          Margin="8"
                                          Content="{Binding (ItemsControl.AlternationIndex),
                                                RelativeSource={RelativeSource Mode=TemplatedParent},
                                                Converter={StaticResource CountNumConverter}}"
                                          Style="{StaticResource WaferButton}" 
                                          IsChecked="{Binding isChecked}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="Click">
                                        <i:InvokeCommandAction Command="{Binding Command}"
                                                   CommandParameter="{Binding CommandParameter}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>

        </Grid>

    </Grid>
</UserControl>
