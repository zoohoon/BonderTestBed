<UserControl x:Class="SemicsOCRSubSettingView.SemicsOCRSubSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cui="clr-namespace:CUI;assembly=CUI"
             xmlns:recipe="clr-namespace:RecipeEditorControl.RecipeEditorParamEdit;assembly=RecipeEditorControl"
             xmlns:local="clr-namespace:SemicsOCRSubSettingView"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1280">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SettingControlStyle;component/SettingViewStyleDictionaly.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="12*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Semics OCR Setup" Foreground="White" FontWeight="Bold" FontSize="24" Height="42"/>
            <cui:Button Grid.Column="2" 
                        Content="Setup" 
                        FontSize="32" 
                        Margin="8"
                         Height="42"
                        GUID="f6873f0c-5bcd-48be-898b-6c03d86d1d1c"
                        Style="{DynamicResource NormalTypeCUIButtonStyle}" 
                        Command="{Binding OCRSetupCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource Self}}"/>
        </Grid>

        <StackPanel Grid.Row="2">
            <Separator Grid.Row="2" Background="White"/>
        </StackPanel>
        <recipe:UcRecipeEditorParamEdit Grid.Row="1" DataContext="{Binding RecipeEditorParamEdit}" Margin="0"/>
    </Grid>
</UserControl>
