<UserControl x:Class="OCRSubSettingView.OCRSubSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OCRSubSettingView"
             xmlns:cui="clr-namespace:CUI;assembly=CUI"
             xmlns:recipe="clr-namespace:RecipeEditorControl.RecipeEditorParamEdit;assembly=RecipeEditorControl"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SettingControlStyle;component/SettingViewStyleDictionaly.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.5*"/>
            <RowDefinition Height="12*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="OCR Setup" Foreground="White" FontWeight="Bold" FontSize="24" Height="42"/>
            <!--<cui:Button Grid.Column="2" 
                        Content="Setup" 
                        FontSize="32" 
                        Margin="8"
                         Height="42"
                        GUID="603ede06-438b-415f-97f1-bbbcc21db1b0"
                        Style="{DynamicResource NormalTypeCUIButtonStyle}" 
                        Command="{Binding OCRSetupCommand}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource Self}}"/>-->
        </Grid>

        <StackPanel Grid.Row="2">
            <Separator Grid.Row="2" Background="White"/>
        </StackPanel>
        <recipe:UcRecipeEditorParamEdit Grid.Row="2" DataContext="{Binding RecipeEditorParamEdit}" Margin="0"/>
    </Grid>
</UserControl>
