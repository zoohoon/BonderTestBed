<UserControl x:Class="LoginControl.LoginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoginControl"
             xmlns:wpfsl="http://schemas.softarcs.com/wpfsmartlibrary"
             xmlns:conv="clr-namespace:ValueConverters;assembly=ValueConverters"
             mc:Ignorable="d"
             Margin="-10"
             d:DesignWidth="1280" d:DesignHeight="1024">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!--<ResourceDictionary Source="../WPFSmartLibraryLight35;component/ResourceDictionaries/CommonRD.xaml" />-->
                <ResourceDictionary Source="/WPFSmartLibraryLight35;component/ResourceDictionaries/CommonRD.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <conv:MulitCommandParameterConverter x:Key="MulitCommandParameterConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="LayoutRoot" Background="Black">
        <wpfsl:SmartLoginOverlay Grid.Column="0" x:Name="SmartLoginOverlayControl"
                                    IsUserOptionAvailable="True"
                                    UserName="{Binding UserName}"
                                    Password="{Binding Password}"
                                    UserImageSource="{Binding UserImageSource}"
                                    AdditionalSystemInfo="Locked"
                                    Command="{Binding SubmitCommand}"
                                    CommandParameter="{Binding RelativeSource={RelativeSource Self}}"/>

        <Grid Grid.Column="0" Width="300" HorizontalAlignment="Left" VerticalAlignment="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition Height="6*"/>
            </Grid.RowDefinitions>
            <DataGrid Grid.Row="0" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      ItemsSource="{Binding DataSource}"
                      ScrollViewer.CanContentScroll="True" 
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent">

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Level" IsReadOnly="True" Width="1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.AccountSelectedCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                    <Button.CommandParameter>
                                        <MultiBinding Converter="{StaticResource MulitCommandParameterConverter}">
                                            <Binding/>
                                            <Binding ElementName="SmartLoginOverlayControl"/>
                                        </MultiBinding>
                                    </Button.CommandParameter>
                                    <Button.Template>
                                        <ControlTemplate>
                                            <TextBlock Text="{Binding UserLevel}" FontWeight="Bold" Foreground="White"/>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Name" IsReadOnly="True" Width="1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.AccountSelectedCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                    <Button.CommandParameter>
                                        <MultiBinding Converter="{StaticResource MulitCommandParameterConverter}">
                                            <Binding/>
                                            <Binding ElementName="SmartLoginOverlayControl"/>
                                        </MultiBinding>
                                    </Button.CommandParameter>
                                    <Button.Template>
                                        <ControlTemplate>
                                            <TextBlock Text="{Binding UserName}" FontWeight="Bold" Foreground="White"/>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

                <DataGrid.CellStyle>
                    <Style TargetType="{x:Type DataGridCell}">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#4573F3"/>
                                <!--<Setter Property="Background" Value="Black"/>-->
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <!--<DataGrid.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.ContainerStyle>
                            <Style TargetType="{x:Type GroupItem}">
                                <Setter Property="Margin" Value="0,0,0,5"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type GroupItem}">
                                            <Expander IsExpanded="True" Background="Transparent" BorderBrush="Transparent" Foreground="#FFEEEEEE" BorderThickness="1,1,1,5">
                                                <Expander.Header>
                                                    <DockPanel>
                                                        <TextBlock FontWeight="Bold" Text="Number of Accounts : "/>
                                                        <TextBlock FontWeight="Bold" Text="{Binding Path=ItemCount}"/>
                                                    </DockPanel>
                                                </Expander.Header>
                                                <Expander.Content>
                                                    <ItemsPresenter />
                                                </Expander.Content>
                                            </Expander>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GroupStyle.ContainerStyle>
                    </GroupStyle>
                </DataGrid.GroupStyle>-->
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
