<UserControl x:Class="ProberViewModel.FilterPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProberViewModel"
             xmlns:cuim="clr-namespace:CUIContextMenu;assembly=CUIContextMenu"
             mc:Ignorable="d" 
             xmlns:valueConverters="clr-namespace:ValueConverters;assembly=ValueConverters"
             IsHitTestVisible="True"
             d:DesignHeight="960" d:DesignWidth="1280">
    <UserControl.Resources>
        <valueConverters:BoolToVisibilityConverter x:Key="BtoVConv" />
    </UserControl.Resources>

    <ItemsControl ItemsSource="{Binding RectangleCollection}"
                  Visibility="{Binding IsEnable, Converter={StaticResource BtoVConv}}"
                  Background="{Binding Background}"
                  Opacity="{Binding Opacity}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <Canvas/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="Canvas.Left" Value="{Binding X}"/>
                <Setter Property="Canvas.Top" Value="{Binding Y}"/>
            </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.ContextMenu>
                        <ContextMenu>
                            <cuim:UserContextMenu DataContext="{Binding SourceUI}"/>
                        </ContextMenu>
                    </Grid.ContextMenu>

                    <Rectangle Width="{Binding Width}" Height="{Binding Height}"
                           Fill="{Binding Color}" Opacity="{Binding Opacity}"
                           />

                    <Label Content="{Binding SourceUI.MaskingLevel, UpdateSourceTrigger=PropertyChanged}" Foreground="White"
                           Padding="0"
                           VerticalContentAlignment="Center"
                           HorizontalContentAlignment="Center"
                           />
                </Grid>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>