<UserControl x:Class="MonitoringMainPageView.UcMonitoringMainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MonitoringMainPageView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="Transparent">
    <ScrollViewer>
        <Grid ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="9*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="9*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Row="0" Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="HW Part Name" Foreground="Green" HorizontalAlignment="Center"/>
                <Label Grid.Column="1" Content="Axis" Foreground="Green" HorizontalAlignment="Center"/>
                <Label Grid.Column="3" Content="Error Status" Foreground="Green" HorizontalAlignment="Center"/>
            </Grid>
            <ItemsControl ItemsSource="{Binding HWCheckerInfoList}" Grid.Row="1" Grid.Column="0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="{Binding HWPartName}" HorizontalAlignment="Center"/>
                            <Label Grid.Column="1" Content="{Binding AxisDesc}" HorizontalAlignment="Center"/>
                            <Label Grid.Column="3" Content="{Binding PartChecker.CurrentErrorLevel}" HorizontalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid Grid.Row="1" Grid.Column="1">
                <Grid Margin="0, 100, 0 ,0">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Row="0" Grid.Column="1" Command="{Binding UpCommand}"/>
                    <Button Grid.Row="1" Grid.Column="0" Command="{Binding LeftCommand}"/>
                    <Button Grid.Row="1" Grid.Column="1" Command="{Binding StopCommand}"/>
                    <Button Grid.Row="1" Grid.Column="2" Command="{Binding RightCommand}"/>
                    <Button Grid.Row="2" Grid.Column="1" Command="{Binding DownCommand}"/>
                    <Button Grid.Row="3" Grid.Column="0" Content="LOCK" Command="{Binding LockCommand}"/>
                    <Button Grid.Row="3" Grid.Column="1" Content="EMERGENCY" Command="{Binding EmergencyCommand}"/>
                    <Button Grid.Row="3" Grid.Column="2" Content="Resume" Command="{Binding ResumeCommand}"/>
                </Grid>
            </Grid>
        </Grid>
    </ScrollViewer>

</UserControl>
