<UserControl x:Class="WASettingView_Standard.Design.InfoGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2.0*"/>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" Margin="4,8,4,8">
            <Label  Content="{Binding ElementID}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Foreground="Black"/>
        </Border>
        <Border Grid.Column="1" Background="White" Margin="4,8,4,8">
            <Label  Content="{Binding Name}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Foreground="Black"/>
        </Border>
        <Border Grid.Column="2" Background="White" Margin="4,8,4,8">
            <Label  Content="{Binding Description}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Foreground="Black"/>
        </Border>
        <Border Grid.Column="3" Background="White" Margin="4,8,4,8">
            <Label  Content="{Binding MinMax}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Foreground="Black"/>
        </Border>

    </Grid>
</UserControl>
