<dialogs:CustomDialog x:Class="PMIProcesser.ManualPMISetupControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:PMIProcesser"
             xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:conv="clr-namespace:ValueConverters;assembly=ValueConverters"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             mc:Ignorable="d" Height="880" Width="800"
             Background="#FF232222">

    <dialogs:CustomDialog.Resources>
        <conv:BooleanToIntConverter             x:Key="BooleanToIsCheckedConverter"/>
        <conv:EnumBooleanConverter              x:Key="EnumBooleanConverter"/>
        <conv:MulitCommandParameterConverter    x:Key="MulitCommandParameterConverter"/>
    </dialogs:CustomDialog.Resources>

    <Grid Margin="-280 0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Width="790">
        <Grid.Resources>
            <ResourceDictionary>
                <System:Double x:Key="DialogTitleFontSize">26</System:Double>
                <System:Double x:Key="DialogMessageFontSize">15</System:Double>
                <Style x:Key="TitleLabel" TargetType="Label">
                    <Setter Property="Margin" Value="0 8 0 0"/>
                    <Setter Property="Background" Value="DodgerBlue" />
                    <Setter Property="FontSize" Value="30" />
                    <Setter Property="Foreground" Value="#FFE8E8E8"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
                <Style x:Key="DefaultButton" TargetType="Button">
                    <Setter Property="Margin" Value="10"/>
                    <Setter Property="FontSize" Value="35" />
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
                <Style x:Key="ExtraButton" TargetType="Button">
                    <Setter Property="Margin" Value="4"/>
                    <Setter Property="FontSize" Value="24" />
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
            </ResourceDictionary>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.8*" MinHeight="20" />
            <RowDefinition Height="9*" />
            <RowDefinition Height="1*" MinHeight="40"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.ColumnSpan="4" Style="{StaticResource TitleLabel}" Content ="Normal PMI Setup" Width="780"/>

        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" BorderBrush="LightGray" BorderThickness="1" Margin="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition MaxHeight="60"/>
                    <RowDefinition MaxHeight="60"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Grid.Row="1" Grid.Column="0" Content="PREV" Style="{StaticResource ExtraButton}"/>
                <Button Grid.Row="1" Grid.Column="1" Content="Next" Style="{StaticResource ExtraButton}"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="2" Grid.ColumnSpan="2" BorderBrush="LightGray" BorderThickness="1" Margin="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition MaxHeight="60"/>
                    <RowDefinition MaxHeight="60"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Grid.Row="1" Grid.Column="0" Content="PREV" Style="{StaticResource ExtraButton}"/>
                <Button Grid.Row="1" Grid.Column="1" Content="Next" Style="{StaticResource ExtraButton}"/>
                <Button Grid.Row="2" Grid.Column="0" Content="APPLY ALL" Style="{StaticResource ExtraButton}"/>
                <Button Grid.Row="2" Grid.Column="1" Content="APPLY" Style="{StaticResource ExtraButton}"/>
            </Grid>
        </Border>

        <Button Style="{StaticResource DefaultButton}" Content="EXIT"
                        Command="{Binding CmdExitClick}"
                        Grid.Row="2" Grid.Column="3" />
    </Grid>
</dialogs:CustomDialog>
