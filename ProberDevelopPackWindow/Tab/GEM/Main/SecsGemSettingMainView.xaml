<UserControl x:Class="ProberDevelopPackWindow.Tab.SecsGemSettingMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProberDevelopPackWindow.Tab"
             xmlns:secsgem="clr-namespace:UcSecsGemSettingDialog;assembly=UcSecsGemSettingDialog"
             mc:Ignorable="d" 
             Height="976" Width="1140">
    <TabControl>
        <TabControl.Resources>
            <Style TargetType="TabItem">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="Gainsboro" CornerRadius="4,4,0,0" Margin="2,0">
                                <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,2"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="LightSkyBlue" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="False">
                                    <Setter TargetName="Border" Property="Background" Value="GhostWhite" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </TabControl.Resources>
        <TabItem Header="COMM" BorderBrush="Black" BorderThickness="1">
            <Border BorderBrush="DimGray" BorderThickness="0.6" Margin="0,0,0,2">
                <secsgem:SecsGemSettingDialogView 
                    Height="680" Width="1120" VerticalAlignment="Top"/>
            </Border>
        </TabItem>
        <TabItem Header="EVENT">
            <Border BorderBrush="DimGray" BorderThickness="0.6"  Margin="0,0,0,2">
                <secsgem:SecsGemEventSettingDialogView Height="940" Width="1120"/>
            </Border>
        </TabItem>
        <TabItem Header="VID Editor">
            <Border BorderBrush="DimGray" BorderThickness="0.6"  Margin="0,0,0,2">
                <local:GemVidsEditorView/>
            </Border>
        </TabItem>
        <TabItem Header="Param Transfer">
            <Border BorderBrush="DimGray" BorderThickness="0.6"  Margin="0,0,0,2">
                <local:GemParamTransferView/>
            </Border>
        </TabItem>
    </TabControl>
</UserControl>
