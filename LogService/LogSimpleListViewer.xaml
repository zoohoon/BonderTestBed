<UserControl x:Class="LogService.LogSimpleListViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:LogService"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">


    
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

        <ScrollViewer x:Name="logScrollViewer" 
                                          CanContentScroll="True"
                                          VerticalScrollBarVisibility="Hidden" 
                                          HorizontalScrollBarVisibility="Hidden">
            <ListView x:Name="logsimpleview" 
                  ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                  >

                <i:Interaction.Behaviors>
                    <local:IgnoreMouseWheelBehavior />
                </i:Interaction.Behaviors>

                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Stretch" />

                        <!--<Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="IsSelected" Value="fa"/>
                        </Trigger>
                    </Style.Triggers>-->

                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock TextWrapping="NoWrap" FontSize="16" Height="24">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0:hh:mm} {1}">
                                    <Binding Path="TimeStamp"/>
                                    <Binding Path="Message"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
        </ScrollViewer>
        
        <!--<ListBox x:Name="logListBox" Background="Transparent" Foreground="White" ItemsSource="{Binding Collection}">
            --><!--<ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="ToolTip" Value="{Binding Message}"/>
                </Style>
            </ListBox.ItemContainerStyle>--><!--
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock TextWrapping="NoWrap" FontSize="12">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0:yyyy/MM/dd hh:mm tt} {1}">
                                <Binding Path="TimeStamp"/>
                                <Binding Path="Message"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>-->
        
        <!--<ScrollViewer Name="logListBoxScroll" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="Visible">
            <ListBox x:Name="logListBox" Background="Transparent" Foreground="White">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="ToolTip" Value="{Binding Message}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock TextWrapping="Wrap" FontSize="15">
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0:yyyy/MM/dd hh:mm tt} {1}">
                                    <Binding Path="TimeStamp"/>
                                    <Binding Path="Message"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>-->

        <!--<ListView Name="logListView"
                  VerticalAlignment="Center" HorizontalAlignment="Stretch"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ScrollViewer.VerticalScrollBarVisibility="Disabled">
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="ToolTip" Value="{Binding Message}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock TextWrapping="Wrap" FontSize="15">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0:yyyy/MM/dd hh:mm tt} {1}">
                                <Binding Path="TimeStamp"/>
                                <Binding Path="Message"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
    </Grid>
</UserControl>
