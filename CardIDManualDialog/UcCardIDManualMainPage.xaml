<UserControl x:Class="CardIDManualDialog.UcCardIDManualMainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ed="http://schemas.microsoft.com/expression/2010/drawing" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:CardIDManualDialog"
              xmlns:vk="clr-namespace:VirtualKeyboardControl;assembly=VirtualKeyboardControl"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800" Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded">
    <Border BorderBrush="Gray" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="11*"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="12*"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Background="WhiteSmoke" Grid.Column="0" Margin="3" TextAlignment="Center" Text="Probe Card ID Manual Input" FontSize="24">
                </TextBlock>
                <TextBlock x:Name="exitBtn" Background="WhiteSmoke" Text="X" Grid.Column="1" Margin="3" TextAlignment="Center" FontSize="24" MouseDown="exitBtn_MouseDown" MouseUp="exitBtn_MouseUp" MouseLeave="exitBtn_MouseLeave">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseUp">
                            <i:InvokeCommandAction Command="{Binding ExitCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </TextBlock>
            </Grid>

            <!--<Grid x:Name="CognexDisplay"  Grid.Row="0" Grid.Column="0"/>-->
            <vk:UcVirtualKeyboard Grid.Row="1"
                            TextData="{Binding CardIDString, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                            EnterKeyCommand="{Binding EnterKeyCommand}" TextBrush="White" TitleBarVisible="Collapsed" 
                            MaxCharLen="30"/>
        </Grid>
    </Border>
</UserControl>
