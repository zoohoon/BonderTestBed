<UserControl x:Class="CardChangeCreator.OrderSettingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CardChangeCreator"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="720" d:DesignWidth="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="144*"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Content="List"/>
            <ListBox x:Name="lbBehaviorList" Grid.Row="1" ItemsSource="{Binding BehaviorCollection}"
                     PreviewMouseLeftButtonDown="ListBox_PreviewMouseLeftButtonDown"
                     />
        </Grid>

        <Grid Grid.Column="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="144*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="Order"/>
                <Button Grid.Column="1" Margin="8, 0, 8, 0" Content="▲▲" Tag = "FirstOfOrder" Click="OrderChange_Click"/>
                <Button Grid.Column="2" Margin="8, 0, 8, 0" Content="▲"  Tag = "OnceUP" Click="OrderChange_Click"/>
                <Button Grid.Column="3" Margin="8, 0, 8, 0" Content="▼ " Tag = "OnceDown" Click="OrderChange_Click"/>
                <Button Grid.Column="4" Margin="8, 0, 8, 0" Content="▼▼" Tag = "EndOfOrder" Click="OrderChange_Click"/>

            </Grid>

            <ListBox x:Name="lbUnloadOrderList" Grid.Row="1" Tag="Unload"
                     ItemsSource="{Binding OrderCollection}"
                     AllowDrop="True" Drop="lbOrderList_Drop" GotFocus="lb_GotFocus"
                     >
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                        <EventSetter Event="MouseDoubleClick" Handler="ListBoxItem_MouseDoubleClick"></EventSetter>
                    </Style>
                </ListBox.ItemContainerStyle>

                <!--<ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="1" x:Name="BackgroundGrid">
                            <TextBlock Grid.Column="1" Text="{Binding }"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>-->
            </ListBox>


        </Grid>
    </Grid>
</UserControl>
