<UserControl x:Class="CardChangeCreator.DesideNextBehaviorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:p="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
             xmlns:local="clr-namespace:CardChangeCreator"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1000">

    <UserControl.Resources>
        <local:BehaviorIdMaker x:Key="BehaviorIdMakerConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Order"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding OrderCardChangeCollection}"
                  AutoGenerateColumns="False"
                  Name="dgOrderCollection">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Behavior Name" Width="80*" Binding="{Binding}"/>
                <DataGridTemplateColumn Header="Positive" Width="80*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox   IsEnabled="True"
                                        ItemsSource="{Binding Path=DataContext.OrderCardChangeCollection, 
                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}}" 
                                        >
                                <ComboBox.SelectedItem>
                                    <MultiBinding Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"
                                                  Converter="{StaticResource BehaviorIdMakerConverter}"
                                                  ConverterParameter="{x:Reference Name=dgOrderCollection}"
                                                  >
                                        <Binding Path="NextID_Positive"/>
                                        <Binding Path="DataContext.OrderCardChangeCollection"
                                                 RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType={x:Type DataGrid}}"/>
                                    </MultiBinding>
                                </ComboBox.SelectedItem>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Negative" Width="80*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox   IsEnabled="True"
                                        ItemsSource="{Binding Path=DataContext.OrderCardChangeCollection, 
                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGrid}}}" 
                                        >
                                <ComboBox.SelectedItem>
                                    <MultiBinding Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"
                                                  Converter="{StaticResource BehaviorIdMakerConverter}"
                                                  ConverterParameter="{x:Reference Name=dgOrderCollection}"
                                                  >
                                        <Binding Path="NextID_Negative"/>
                                        <Binding Path="DataContext.OrderCardChangeCollection"
                                                 RelativeSource="{RelativeSource Mode=FindAncestor, AncestorType={x:Type DataGrid}}"/>
                                    </MultiBinding>
                                </ComboBox.SelectedItem>
                            </ComboBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
