<dialogs:CustomDialog x:Class="WAPadStandardModule.UC.PadAdvenSettingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WAPadStandardModule.UC"
              xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Label Grid.Row="0" VerticalAlignment="Bottom" Content="Seleted PadType" Foreground="Black" FontWeight="DemiBold"/>
            <ComboBox Height="50" Margin="8,2,0,2" HorizontalAlignment="Left" x:Name="combo_padtype" Width="200"
                   SelectedItem="{Binding PadType}"/>
        </StackPanel>
        <StackPanel Grid.Row="1">
            <Label Grid.Row="0" VerticalAlignment="Bottom" Content="Input BumpPad Height" Foreground="Black" FontWeight="DemiBold"/>
            <TextBox Height="50" Margin="8,2,0,2" HorizontalAlignment="Left"  Width="200" Style="{DynamicResource ErrorInfoTextBoxStyle}" 
                       Text="{Binding BumpPadHeight}" IsEnabled="{Binding TBBumpPadHeightEnable}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="PreviewMouseDown" >
                            <i:InvokeCommandAction Command="{Binding TextBoxClickCommand}"
                                                   CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TextBox}}}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
            </TextBox>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Height="45" Content="OK" Margin="4" FontSize="15" Click="OK_Button_Click" Width="80" HorizontalAlignment="Right" />
            <Button Height="45" Content="CANCEL" Margin="4" FontSize="15" Click="Cancel_Button_Click" Width="80" HorizontalAlignment="Right" />
        </StackPanel>
    </Grid>
</dialogs:CustomDialog>
