<UserControl x:Class="NeedleCleanViewer.NeedleCleanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:sharprender="clr-namespace:SharpDXRender;assembly=SharpDXRender"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeedleCleanViewer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="940"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Margin="8">
            <Grid>
                <sharprender:RenderControl RenderLayerData="{Binding MainRenderLayer}" />
                <!--<sharprender:RenderControl RenderLayerData="{Binding MainRenderLayer}" />-->
            </Grid>
        </Border>
    </Grid>
</UserControl>
