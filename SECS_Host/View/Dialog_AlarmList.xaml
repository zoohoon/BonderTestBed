<Window x:Class="SECS_Host.View.Dialog_AlarmList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SECS_Host.View"
        xmlns:boollist =" "
        mc:Ignorable="d"
        Title="Dialog_AlarmList" Height="640" Width="960"  Closing="Window_Closing">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DataGrid   Margin="8"  ItemsSource="{Binding AlarmCollection}"
                    AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn     Header="ALID"           Width="8*"
                                        IsReadOnly="True"
                                        Binding="{Binding ALID}"
                                        />
                <DataGridTextColumn     Header="ALCD"           Width="8*"
                                        IsReadOnly="True"
                                        Binding="{Binding ALCD}"
                                        />
                <DataGridTextColumn     Header="ALTX"           Width="56*"
                                        Binding="{Binding ALTX}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Background" Value="#5C95DE58"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="IsAble"    Width="16*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Button Content="{Binding IsAble}" Command="{Binding ChangeAlarmAbleCommand}"/>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="24*"/>
                <RowDefinition Height="40*"/>
                <RowDefinition Height="40*"/>
                <RowDefinition Height="40*"/>
                <RowDefinition Height="40*"/>
            </Grid.RowDefinitions>
            <Button     Grid.Column="1"     Margin="8"
                        Command="{Binding CloseWindowCommand}"
                        Content="Close"/>
        </Grid>
    </Grid>
</Window>
