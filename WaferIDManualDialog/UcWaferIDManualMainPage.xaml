<UserControl x:Class="WaferIDManualDialog.UcWaferIDManualMainPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ed="http://schemas.microsoft.com/expression/2010/drawing" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:WaferIDManualDialog"
              xmlns:vk="clr-namespace:VirtualKeyboardControl;assembly=VirtualKeyboardControl"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800" Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded">
    <Border BorderBrush="Gray" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="1.5*"/>
                <RowDefinition Height="13*"/>
            </Grid.RowDefinitions>


            <Grid Grid.Row="0">
                <Border Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="2"  Margin="3" Background="WhiteSmoke">
                    <TextBlock  Grid.Row="0" Margin="3" TextAlignment="Center" Text="Wafer ID Manual Input" FontSize="24"/>
                </Border>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="12*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
               
                <Border Grid.Row="1"  Grid.Column="0"  Margin="3" Background="WhiteSmoke">
                    <TextBlock Text="{Binding TargetInfo}" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="20"></TextBlock>
                </Border>


                <TextBlock x:Name="exitBtn" Background="WhiteSmoke" Text="X" Grid.Row="1" Grid.Column="1" Margin="3" TextAlignment="Center" FontSize="24" MouseDown="exitBtn_MouseDown" MouseUp="exitBtn_MouseUp" MouseLeave="exitBtn_MouseLeave">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseUp">
                            <i:InvokeCommandAction Command="{Binding ExitCommand}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </TextBlock>
            </Grid>

            <!--<Grid x:Name="CognexDisplay"  Grid.Row="0" Grid.Column="0"/>-->
            <vk:UcVirtualKeyboard Grid.Row="2"
                            TextData="{Binding WaferIDString, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"                            
                            EnterKeyCommand="{Binding EnterKeyCommand}" TextBrush="White" TitleBarVisible="Collapsed" 
                            MaxCharLen="30"/>
        </Grid>
    </Border>
</UserControl>
